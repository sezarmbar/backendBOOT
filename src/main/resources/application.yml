# config context path to "/" by setting an empty string
server:
  contextPath:

# JACKSON
#spring:
#  jackson:
#    serialization:
#      INDENT_OUTPUT: true

jwt:
  header: Authorization
  secret: OldenburgSecret
  expiration: 604800
  route:
    authentication:
      path: auth
      refresh: refresh

#logging:
#  level:
#    org.springframework:
#      security: DEBUG
#spring.datasource.url: jdbc:mysql://localhost:3306/umfrage
spring.datasource.url: jdbc:mysql://localhost:3306/test
#spring.datasource.url: jdbc:mysql://localhost:3306/concretepage?useSSL=false


spring.datasource.username: root
spring.datasource.password: root
#spring.datasource.username: umfrage
#spring.datasource.password: stol99
#spring.datasource.password:
spring.jpa.hibernate.ddl-auto: update
spring.datasource.driver-class-name: com.mysql.jdbc.Driver
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect